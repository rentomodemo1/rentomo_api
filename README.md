# Rentomo API

HTTP service for payments and vehicle availability. Run `go run ./cmd/api`; the service listens on localhost:8081. Run `go test ./...` to check behavior. Payments use an injectable in-memory function; no credentials or external services are needed.
